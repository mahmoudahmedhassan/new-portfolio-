/* eslint-disable react-refresh/only-export-components */
import { resume } from "../assets";

 
 
const CV = () => {
  return (
    <div>
       <a href={resume} className="bg-[rgb(110,72,170)] text-white px-4 py-2 rounded" title="" download>Download CV</a>
    </div>
  );
};

export default CV;

